// #include "render_device.h"

// void
// RenderDevice::Draw(Shader& shader, u32& vertex_array)
// {
//     shader.Bind();
// }

// void
// RenderDevice::Clear(v3& color, b32 clear_depth_buffer)
// {

// }

// Shader*
// RenderDevice::CreateShader(const char* vs, const char* fs)
// {
//     Shader shader(vs, fs);
//     shaders.emplace_back(shader);
//     return &shaders[shaders.size()-1];
// }

// Texture2D*
// RenderDevice::RenderCreateTexture(const char* filename, TextureFilter filer, TextureWrap wrapping, TextureType type, u32 slot)
// {
//     Texture2D texture(filename, filter, wrapping, type);
//     textures.emplace_back(texture);

//     return &textures.[textures.size()-1];
// }